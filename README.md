## Custom Caddy Docker build to integrate various plugins

- rate_limit from github.com/mholt/caddy-ratelimit

Caddy docker image is built using the Dockerfile. The base image and the plugin Repos are regularly checked for updates and, if found, the Docker image is rebuilt and pushed to Docker hub.
